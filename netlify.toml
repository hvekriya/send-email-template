[build]
  command = "npm run generate"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/api/clicks"
  to = "/.netlify/functions/clicks"
  status = 200
  force = true
